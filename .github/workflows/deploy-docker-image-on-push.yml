name: Build and deploy a docker image on push

on:
  push:
    paths-ignore:
    - '**.yml'
    - 'README.md'
    
jobs:
  
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Docker Login
      uses: azure/docker-login@v1
      with:
        login-server: docker.pkg.github.com
        username: $GITHUB_ACTOR
        password: ${{ secrets.GITHUB_TOKEN }}
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag docker.pkg.github.com/${{github.repository}}/peaks:rainier
    - name: Push Docker Image  
      run: docker push docker.pkg.github.com/${{github.repository}}/peaks:rainier


